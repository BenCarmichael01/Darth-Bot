/* eslint-disable no-unused-vars */
/* global __base */

module.exports = {
	name: 'test',
	description: 'test',
	category: 'testing',
	testOnly: true,
	permissions: ['ADMINISTRATOR'],

	async callback({ message, guild, instance, client }) {},
};

